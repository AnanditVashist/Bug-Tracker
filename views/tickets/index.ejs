<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
        <% for (let ticket of tickets) {%>
            <li><%= ticket.title %>- <%= ticket.description %>-
                <a href="/tickets/details/<%= ticket._id %> ">Details</a>
                <a href="/tickets/edit/<%= ticket._id %>">Edit</a>
                <form action="/tickets/delete/<%= ticket._id %>?_method=DELETE" method="post">
                    <button type="submit">Delete</button>
                </form>
            </li>
    </ul>
        <% } %> 
</body>
</html>